//Does this code produce an error? Why/why not and what is the output to console?
for (let i = 0; i < 5;) {
    console.log(i += 1);
  }

//It doesn't produce an error because the increment statement is still there and the components of a foor loop are optional. It logs 1-5