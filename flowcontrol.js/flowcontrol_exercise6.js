//What does this code output to console
function isArrayEmpty(arr) {
    if (arr) {
      console.log('Not Empty');
    } else {
      console.log('Empty');
    }
  }
  
  isArrayEmpty([]);

//This logs 'Not Empty' because there is an empty array in the argument and not false