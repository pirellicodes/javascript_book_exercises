//Identify the following, function arguments, function body, function declaration, function name, parameters, function return value and the names of all variables
function multiplyNumbers(num1, num2, num3) {
    let result = num1 * num2 * num3;
    return result;
  }
  
  let product = multiplyNumbers(2, 3, 4);

  //function arguments: 2, 3, 4
  //function body: function multiplyNumbers(num1, num2, num3) {}
  //function dec: function multiplyNumbers()
  //function invocation: multiplyNumbers(2, 3, 4)
  //function name: multiplyNumbers
  //function params: num1, num2, num3
  //function return value: in results variable then gets assigned to product after return
  //all variable names: num1, num2, num3, results and product, multiplyNumbers